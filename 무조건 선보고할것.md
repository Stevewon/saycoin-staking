# ⚠️ 작업 규칙 — 데이터 관련 무조건 선(先)보고

> 사장님(보스) 지시 / 2026-06-16

## 핵심 원칙
**언어(i18n)를 바꾸라고 하면 언어만 바꾼다. 데이터는 절대 건드리지 않는다.**

## 절대 규칙
1. **데이터를 건드릴 가능성이 조금이라도 있으면 → 작업 전 무조건 사장님께 먼저 보고하고 승인을 받는다.**
   - 추측으로 데이터 관련 코드를 수정/배포하지 않는다.
   - "혹시 데이터에 영향이 갈 수도 있다" 싶으면 손대기 전에 멈추고 보고한다.

2. **"데이터"의 범위 (아래는 전부 선보고 대상)**
   - DB(D1) 스키마, 테이블, 컬럼
   - 잔액/수당/배당/거래내역/회원정보 등 사용자 데이터
   - 데이터를 조회/표시/계산하는 API 및 그 호출 로직
   - 대시보드의 잔액·숫자 표시 로직
   - cron / 배당 실행 / 회수 등 데이터를 변경하는 기능

3. **언어 작업(i18n)에서 허용되는 범위 (선보고 없이 가능)**
   - `public/static/i18n.js` 의 번역 사전(키-값) 추가/수정
   - HTML 요소의 `data-i18n` / `data-i18n-placeholder` / `data-i18n-title` / `data-i18n-html` 속성
   - 화면에 보이는 **고정 텍스트 문구**의 다국어 처리
   - i18n.js 캐시버스트 버전(`?v=...`)
   - ※ 단, 이때도 **데이터 조회/표시 로직(변수, API, 계산)에는 절대 손대지 않는다.**

## 이번 사고에서 얻은 교훈
- i18n 텍스트를 alert 안에 넣으면서 줄바꿈 이스케이프(`\n` vs `\\n`)를 잘못 써서
  대시보드 스크립트 전체가 죽고 → 데이터가 전부 0으로 보임.
- **언어만 바꾼다고 했지만 결과적으로 데이터 표시가 망가짐.**
- 앞으로 언어 작업이라도 **스크립트 실행에 영향을 줄 수 있는 코드**는
  배포 전 반드시 빌드 산출물 문법검사 + 실제 페이지 렌더 검증을 거친다.

## 작업 전 체크리스트 (i18n 작업 시 매번)
- [ ] 내가 건드리는 게 "텍스트 문구"인가, 아니면 "데이터/로직"인가?
- [ ] 데이터/로직이면 → 멈추고 사장님께 선보고
- [ ] 텍스트 문구만이면 → 진행하되, 배포 전:
  - [ ] `node --check public/static/i18n.js`
  - [ ] `npm run build` 성공
  - [ ] 빌드 worker 깨진 문자열 리터럴 스캔 0건
  - [ ] 실제 페이지 렌더(콘솔 에러 0건) 확인
- [ ] 배포 후 운영 페이지에서 데이터 정상 표시 재확인
